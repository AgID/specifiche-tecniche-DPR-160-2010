openapi: 3.0.3

info:
  title: Catalogo SSU Metadata
  version: 1.0.0
  description: |-
    API rese disponibili dal Catalogo SSU alle componenti FrontOffice/BackOffice e Enti Terzi per la consultazione dei metadati

  contact:
    name: AgID - Agenzia per l'Italia Digitale
    url: https://www.agid.gov.it/
  license:
    name: CC BY-NC-SA 4.0
    url: https://creativecommons.org/licenses/by-nc-sa/4.0/

servers:
  - url: https://<system host+subdomains>.it
    description: Catalogo SSU Metadata endpoint

paths:
  /context:
    get:
      description: Recupero dei possibili valori del contesto di riferimento

      responses:
        "200":
          description: Elenco dei possibili valori del contesto di riferimento
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - code
                    - description
                    - sub_context
                  properties:
                    code:
                      title: contesto di riferimento
                      type: string
                    description:
                      title: descrizione del contesto di riferimento (es. ZES)
                      type: string
                    sub_context:
                      title: Specificità del contesto (es. ZES)
                      type: string

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Context
  /tags:
    get:
      description: Recupero dei possibili valori del contesto di riferimento

      responses:
        "200":
          description: Elenco dei possibili valori dei tag dei procedimenti
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - tag
                    - description
                  properties:
                    tag:
                      title: id del tag
                      type: string
                    description:
                      title: descrizione del tag
                      type: string


          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Tags
        
  /typologies-lifeevents/{context}:
    get:
      description: Recupero elenco tipologie ed eventi della vita dei procedimenti
      parameters:
        - in: query
          name: typology
          description: parametro di filtering sulla tipologia richiesta, non valorizzato per richiedere l'intero set
          required: false
          schema:
            type: string
        - name: lifeevent
          in: query
          description: parametro di filtering sugli eventi della vita, non valorizzato per richiedere l'intero set
          required: false
          schema:
            type: string
        - name: municipality
          in: query
          description: parametro di filtering sul comune richiesto, non valorizzato per richiedere l'intero set
          required: false
          schema:
            type: string
        - in: path
          name: context
          description: contesto di riferimento. I valori ammissibili sono reperibili invocando il path /context
          required: true
          schema:
            type: string
            default: "SUAP"

      responses:
        "200":
          description: Elenco delle tuple {typology,lifeevent,municipality} filtrato in base ai paramentri di richiesta
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - typology
                    - lifeevent
                    - municipality
                    - context
                  properties:
                    typology:
                      description: tipologia del procedimento (es. esercizio di vicinato, grande/media struttura di vendita)
                      type: string
                    lifeevent:
                      description: evento della vita relativo al procedimento (apertura, ampliamento, subingresso,etc.)
                      type: string
                    municipality:
                      description: identificativo del comune per cui la coppia  {typology,lifeevent} esiste per almeno un procedimento afferente afferente al comune
                      type: string
                    context:
                      description: contesto di riferimento (es. SUAP, SUE, ZES)
                      type: string

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: risorsa non trovata
          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Lookup
  
  /administrative-regimes/{context}:
    get:
      description: Recupero elenco dei regimi amministrativi legati ad uno specifico contesto
      parameters:
        - in: query
          name: id
          description: paramentro di filtering sull'identificativo del regime amministrativo, se non valorizzato restituisce l'intero set
          required: false
          schema:
            type: string
        - in: query
          name: version
          description: paramentro di filtering sulla versione del regime amministrativo, se non valorizzato restituisce l'intero set
          required: false
          schema:
            type: string
        - in: path
          name: context
          description: contesto di riferimento. I valori ammissibili sono reperibili invocando il path /context
          required: true
          schema:
            type: string
            default: "SUAP"

      responses:
        "200":
          description: Elenco delle tuple {id, version, description, context} filtrato in base ai paramentri di richiesta
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - id
                    - version
                    - description
                    - context
                  properties:
                    id:
                      description: identificativo del regime amministrativo nell'ambito del contesto (es. SCIA, AUTORIZZAZIONE, SILENZIO ASSENSO, COMUNICAZIONE)
                      type: string
                    version:
                      description: versione del regime amministrativo nell'ambito del contesto
                      type: string
                    description:
                      description: descrizione del regime amministrativo nell'ambito del contesto
                      type: string
                    context:
                      description: contesto di riferimento (es. SUAP, SUE, ZES)
                      type: string

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: risorsa non trovata
          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Lookup

  /proceedings/{context}:
    get:
      description: Recupero elenco procedimenti per tipologia,evento della vita e comune (mapping su ambito territoriale)
      parameters:
        - in: query
          name: typology
          description: Recupero elenco procedimenti per tipologia, evento della vita, comune (mapping su ambito territoriale) e contesto di riferimento
          schema:
            type: string
        - name: lifeevent
          in: query
          description: Evento della vita relativo al procedimento (apertura, ampliamento, subingresso,etc.)
          schema:
            type: string
        - name: municipality
          in: query
          description: identificativo del comune necessario a determinare l'ambito territoriale del procedimento
          required: true
          schema:
            type: string
        - in: path
          name: context
          description: contesto di riferimento. I valori ammissibili sono reperibili invocando il path /context
          required: true
          schema:
            type: string
            default: "SUAP"

      responses:
        "200":
          description: Elenco dei procedimenti richiesti
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Proceeding"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Proceedings
 
  /proceeding/{proceeding_id}/{proceeding_version}/usecase-proceedings:
    get:
      description: Recupero elenco fattispecie (usecase-proceedings) di uno specifico procedimento
      parameters:
        - in: path
          name: proceeding_id
          description: id del procedimento
          required: true
          schema:
            type: string
        - name: proceeding_version
          in: path
          description: versione del procedimento
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"

      responses:
        "200":
          description: Elenco delle fattispecie relative al procedimento richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/UseCaseProceeding"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Proceedings

  /document_classifications:
    get:
      description: |
        Recupero di tutte le classificazioni che possono essere associate ai documenti secondo la Tassonomia in Allegato A - Art.5 comma 1 Decreto 17 settembre 2024 n.159 ss.mm.ii.

      parameters:
        - name: coding
          in: query  
          description: Codifica della classificazione associata ad un documento
          required: false
          schema:
            type: string
            pattern: "C[0-9]{1,2}_[a-z]"
        - name: class_code
          in: query  
          description: Codice della Classe di un documento
          required: false
          schema:
            type: string
            pattern: "C[0-9]{1,2}"
        - name: class_description
          in: query  
          description: Descrizione della Classe di un documento
          required: false
          schema:
            type: string
        - name: typology_code
          in: query  
          description: Codice della Tipologia di un documento
          required: false
          schema:
            type: string
            pattern: "[a-z]"
        - name: typology_description
          in: query  
          description: Descrizione della Tipologia di un documento
          required: false
          schema:
            type: string

      responses:
        "200":
          description: Elenco delle classificazioni che possono essere associate ai documenti.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentClassification"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Classifications

  /general_index_classifications/{name}:
    get:
      description: |
        Recupero della classificazione associata ad un documento del General-Index, secondo la Tassonomia in Allegato A - Art.5 comma 1 Decreto 17 settembre 2024 n.159 ss.mm.ii.

      parameters:
        - name: name
          in: path  
          description: Nome del documento gestito nel General-Index
          required: true
          schema:
            type: string

      responses:
        "200":
          description: Elenco delle classificazioni che possono essere associate ai documenti.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentClassification"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Classifications

  /document_classification/form/{form_id_list}:
    get:
      description: |
        Recupero della classificazione associata ai documenti di tipo Form secondo la Tassonomia in Allegato A - Art.5 comma 1 Decreto 17 settembre 2024 n.159 ss.mm.ii.

      parameters:
        - name: form_id_list
          in: path
          description: lista di id associati ai form
          required: true
          schema:
              type: string

      responses:
        "200":
          description: Elenco dei documenti classificati
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentEntityClassified"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Classifications

  /document_classification/attachment/{attachment_id_list}:
    get:
      description: |
        Recupero della classificazione associata ai documenti di tipo Attachment secondo la Tassonomia in Allegato A - Art.5 comma 1 Decreto 17 settembre 2024 n.159 ss.mm.ii.

      parameters:
        - name: attachment_id_list
          in: path
          description: lista di id associati agli attachment
          required: true
          schema:
              type: string

      responses:
        "200":
          description: Elenco dei documenti classificati
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentEntityClassified"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Classifications

  /proceeding/{proceeding_id}/{proceeding_version}/attachments:
    get:
      description: |
        Recupero elenco allegati di un procedimento

      parameters:
        - in: path
          name: proceeding_id
          description: id del procedimento
          required: true
          schema:
            type: string
        - name: proceeding_version
          in: path
          description: versione del procedimento
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"

      responses:
        "200":
          description: Elenco degli allegati al procedimento richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object 
                  required:
                    - attachment
                    - mandatory
                  properties:
                    attachment:
                      $ref: "#/components/schemas/DocumentEntity"
                    mandatory:
                      type: boolean


          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Proceedings

  /proceeding/{proceeding_id}/{proceeding_version}/{municipality}/competent-administration:
    get:
      description: |
        Recupero amministrazione competente per uno specifico procedimento
      parameters:
        - in: path
          name: proceeding_id
          description: id del procedimento
          required: true
          schema:
            type: string
        - name: proceeding_version
          in: path
          description: versione del procedimento
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"
        - name: municipality
          in: path
          description: identificativo del comune necessario a determinare l'amministrazione competente
          required: true
          schema:
            type: string

      responses:
        "200":
          description: Amministrazione competente del procedimento richiesto
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdministrationSchema"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []
      tags:
        - Proceedings

  /proceeding/{proceeding_id}/{proceeding_version}/form:
    get:
      description: |
        Recupero modulo digitale di uno specifico procedimento

      parameters:
        - in: path
          name: proceeding_id
          description: id del procedimento
          required: true
          schema:
            type: string
        - name: proceeding_version
          in: path
          description: versione del procedimento
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"

      responses:
        "200":
          description: Modulo digitale di uno specifico procedimento
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentEntity"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Proceedings

  /usecase-proceedings/{usecase_proceedings_id}/{usecase_proceedings_version}/usecase-proceedings/{municipality}:
    get:
      description: |
        Recupero elenco delle fattispecie relazionate a una data fattispecie

      parameters:
        - in: path
          name: usecase_proceedings_id
          description: id della fattispecie
          required: true
          schema:
            type: string
        - name: usecase_proceedings_version
          in: path
          description: versione della fattispecie
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"
        - in: path
          name: municipality
          description:   parametro di filtering sul comune richiesto
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Elenco delle fattispecie relazionate alla fattispecie richiesta
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - usecase-proceeding
                    - mandatory
                  properties:
                    usecase-proceeding:
                      $ref: "#/components/schemas/UseCaseProceeding"
                    mandatory:
                      type: boolean

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - UseCases
        
  /usecase-proceeding/{usecase_proceedings_id}/{usecase_proceedings_version}/proceeding:
    get:
      description: |
        Recupero elenco delle fattispecie relazionate a una data fattispecie

      parameters:
        - in: path
          name: usecase_proceedings_id
          description: id della fattispecie
          required: true
          schema:
            type: string
        - name: usecase_proceedings_version
          in: path
          description: versione della fattispecie
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"

      responses:
        "200":
          description: Elenco delle fattispecie relazionate alla fattispecie richiesta
          content:
            application/json:
              schema:
                  $ref: "#/components/schemas/Proceeding"


          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - UseCases        

  /form/{form_id}/{form_version}/xsd:
    get:
      description: |
        Recupero XML Schema di uno specifico modulo digitale

      parameters:
        - in: path
          name: form_id
          description: id del modulo digitale
          required: true
          schema:
            type: string
        - name: form_version
          in: path
          description: versione del modulo digitale
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"

      responses:
        "200":
          description: XMLSchema del modulo digitale richiesto
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceEntity"
             

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Forms

  /form/{form_id}/{form_version}/schematron:
    get:
      description: |
        Recupero Schematron riferito ad un specifico modulo digitale

      parameters:
        - in: path
          name: form_id
          description: id del modulo digitale
          required: true
          schema:
            type: string
        - name: form_version
          in: path
          description: versione del modulo digitale
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"

      responses:
        "200":
          description: Schematron associato al modulo digitale
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceEntity"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Forms

  /attachment/{attachment_id}/{attachment_version}/xsd:
    get:
      description: |
        Recupero XML Schema riferito a un specifico allegato

      parameters:
        - in: path
          name: attachment_id
          description: id dell'allegato
          required: true
          schema:
            type: string
        - name: attachment_version
          in: path
          description: versione dell'allegato
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"

      responses:
        "200":
          description: XML Schema associato all'allegato
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceEntity"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Attachments

  /attachment/{attachment_id}/{attachment_version}/schematron:
    get:
      description: |
        Recupero Schematron riferito ad un specifico allegato

      parameters:
        - in: path
          name: attachment_id
          description: id dell'allegato
          required: true
          schema:
            type: string
        - name: attachment_version
          in: path
          description: versione dell'allegato
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"

      responses:
        "200":
          description: Schematron associato all'allegato
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ResourceEntity"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Attachments
 
  /competent-administration/{competent_administration_ipacode}/{competent_administration_code}/{competent_administration_version}/third-party-system:
    get:
      description: |
        Recupero elenco Enti Terzi di una specifica amministrazione competente

      parameters:
        - in: path
          name: competent_administration_ipacode
          description: codice ipa dell'amministrazione
          required: true
          schema:
            type: string
        - in: path
          name: competent_administration_code
          description: codice dell'ufficio dell'amministrazione
          required: true
          schema:
            type: string
        - name: competent_administration_version
          in: path
          description: versione dell'ufficio amministrazione
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"

      responses:
        "200":
          description: Elenco sistema Ente Terzo dell'amministrazione richiesta
          content:
            application/json:
              schema:
                type: array
                items: 
                  $ref: "#/components/schemas/third_party_system"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Administrations
        
  /competent-administration/{competent_administration_ipacode}/{competent_administration_code}:  
    get:
      description: |
        Recupero elenco Enti Terzi di una specifica amministrazione competente

      parameters:
        - in: path
          name: competent_administration_ipacode
          description: codice ipa dell'amministrazione
          required: true
          schema:
            type: string
        - in: path
          name: competent_administration_code
          description: codice dell'ufficio dell'amministrazione
          required: true
          schema:
            type: string


      responses:
        "200":
          description: Elenco sistema Ente Terzo dell'amministrazione richiesta
          content:
            application/json:
              schema:
                type: array
                items: 
                  $ref: "#/components/schemas/AdministrationSchema"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Administrations        

  /competent-administration/{competent_administration_ipacode}:  
    get:
      description: |
        Recupero elenco Enti Terzi di una specifica amministrazione competente

      parameters:
        - in: path
          name: competent_administration_ipacode
          description: codice ipa dell'amministrazione
          required: true
          schema:
            type: string


      responses:
        "200":
          description: Elenco sistema Ente Terzo dell'amministrazione richiesta
          content:
            application/json:
              schema:
                type: array
                items: 
                  $ref: "#/components/schemas/AdministrationSchema"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Administrations          

  /competent-administration/{competent_administration_ipacode}/{competent_administration_code}/third-party-system:  
    get:
      description: |
        Recupero elenco componenti di una specifica amministrazione competente

      parameters:
        - in: path
          name: competent_administration_ipacode
          description: codice ipa dell'amministrazione
          required: true
          schema:
            type: string
        - in: path
          name: competent_administration_code
          description: codice dell'ufficio dell'amministrazione
          required: true
          schema:
            type: string


      responses:
        "200":
          description: Elenco sistema Ente Terzo dell'amministrazione richiesta
          content:
            application/json:
              schema:
                type: array
                items: 
                  $ref: "#/components/schemas/third_party_system"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Administrations

  /administration/{fiscal_code}/third-party-system:  
    get:
      description: |
        Recupero elenco componenti di una specifica amministrazione competente dato il solo fiscalcode (amminitrazione ghost)

      parameters:
        - in: path
          name: fiscal_code
          description: fiscal_code dell'amministrazione
          required: true
          schema:
            type: string
       


      responses:
        "200":
          description: Elenco sistema Ente Terzo dell'amministrazione richiesta
          content:
            application/json:
              schema:
                type: array
                items: 
                  $ref: "#/components/schemas/third_party_system"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Administrations

  /administration/{fiscal_code}:
    get:
      description: Recupero amministrazione dato il codice fiscale
      parameters:
        - in: path
          name: fiscal_code
          description: codice fiscale dell'amministrazione
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Amministrazione competente richiesta
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdministrationFiscalCode'
          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Administrations

  /single-desk/{municipality}/{context}:
    get:
      description: |
        Recupero Sportello per uno specifico comune

      parameters:
        - in: path
          name: municipality
          description: comune dello sportello
          required: true
          schema:
            type: string
        - in: path
          name: context
          description: contesto di riferimento. I valori ammissibili sono reperibili invocando il path /context
          required: true
          schema:
            type: string
            default: "SUAP"

      responses:
        "200":
          description: Sportello del comune richiesto
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeskEntity"
          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Sportello

  /single-desk/{single_desk_id}/{single_desk_version}/frontoffice-system:
    get:
      description: |
        Recupero Sistema FrontOffice di uno specifico Sportello
      parameters:
        - in: path
          name: single_desk_id
          description: id dello Sportello
          required: true
          schema:
            type: string
        - name: single_desk_version
          in: path
          description: versione dello Sportello
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"

      responses:
        "200":
          description: Sistema FrontOffice dello Sportello richiesto
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FOSystemEntity"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Sportello

  /single_desk/{single_desk_id}/{single_desk_version}/backoffice-system:
    get:
      description: |
        Recupero Sistema BackOffice di uno specifico Sportello
      parameters:
        - in: path
          name: single_desk_id
          description: id dello Sportello
          required: true
          schema:
            type: string
        - name: single_desk_version
          in: path
          description: versione dello Sportello
          required: true
          schema:
            type: string
            pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"

      responses:
        "200":
          description: Sistema BackOffice dello Sportello richiesto
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BOSystemEntity"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Sportello

  /proceedings-by-municipality/{municipality_list}/{context}:
    get:
      description: |
        Recupero bulk elenco procedimenti(tutte le versioni) per una lista di comuni
      parameters:
        - in: path
          name: municipality_list
          description: elenco dei comuni per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
        - in: path
          name: context
          description: contesto di riferimento. I valori ammissibili sono reperibili invocando il path /context
          required: true
          schema:
            type: string
            default: "SUAP"

      responses:
        "200":
          description: Elenco dei procedimenti richiesti
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Proceeding"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk
        
  /proceedings-by-municipality/{municipality_list}/{context}/{tags}:
    get:
      description: |
        Recupero bulk elenco procedimenti(tutte le versioni) per una lista di comuni
      parameters:
        - in: path
          name: municipality_list
          description: elenco dei comuni per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
        - in: path
          name: context
          description: contesto di riferimento. I valori ammissibili sono reperibili invocando il path /context
          required: true
          schema:
            type: string
            default: "SUAP"
        - in: path
          name: tags
          description: elenco dei tag per i quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple            

      responses:
        "200":
          description: Elenco dei procedimenti richiesti
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Proceeding"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk        

  /proceedings-by-id/{id_proceeding_list}:
    get:
      description: |
        Recupero bulk elenco procedimenti(tutte le versioni) per una lista di identificativi procedimento
      parameters:
        - in: path
          name: id_proceeding_list
          description: elenco degli id per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
    


      responses:
        "200":
          description: Elenco dei procedimenti richiesti
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Proceeding"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /proceedings-attachments/{id_proceeding_list}:
    get:
      description: |
         Recupero bulk elenco tuple {procedimento,allegato,obbligatorio}(tutte le versioni) per una lista di identificativi procedimento
      parameters:
        - in: path
          name: id_proceeding_list
          description: elenco degli id per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
    
      responses:
        "200":
          description: Elenco tuple {procedimento,allegato} richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - proceeding
                    - attachment
                    - mandatory
                  properties:
                    proceeding:
                      $ref: "#/components/schemas/ContextEntity"
                    attachment:
                      $ref: "#/components/schemas/DocumentEntity"
                    mandatory:
                      type: boolean

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /proceedings-administrative-regimes/{id_proceeding_list}:
    get:
      description: |
        Recupero bulk elenco tuple {fattispecie, regimi amministrativi e tempi}(tutte le versioni) per una lista di identificativi procedimento
      parameters:
        - in: path
          name: id_proceeding_list
          description:  elenco degli id per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple

      responses:
        "200":
          description: Elenco tuple {fattispecie, regimi amministrativi e tempi} richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - usecase-proceeding
                    - adminstrative-regime
                  properties:
                    usecase-proceeding:
                      $ref: "#/components/schemas/UseCaseProceeding"
                    adminstrative-regime:
                      $ref: "#/components/schemas/AdministrativeRegime"
                    times:
                      $ref: "#/components/schemas/Times"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /proceedings-usecases/{id_proceeding_list}:
    get:
      description: |
        Recupero bulk elenco tuple {procedimento,fattispecie}(tutte le versioni) per una lista di identificativi procedimento
      parameters:
        - in: path
          name: id_proceeding_list
          description: elenco degli id per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
    
      responses:
        "200":
          description: Elenco  tuple {procedimento,fattispecie} richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - proceeding
                    - usecase-proceeding
                  properties:
                    proceeding:
                        $ref: "#/components/schemas/ContextEntity"
                    usecase-proceeding:
                      type: array
                      items:
                        $ref: "#/components/schemas/UseCaseProceeding"
                  

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /usecase-proceedings-endoproceedings/{id_usecase_proceeding_list}/{context}:
    get:
      description: |
         Recupero bulk tuple {fattispecie,fattispecie,obbligatorio}(tutte le versioni) per una lista di identificativi procedimento
      parameters:
        - in: path
          name: id_usecase_proceeding_list
          description: elenco degli id per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
        - in: path
          name: context
          description: contesto di riferimento. I valori ammissibili sono reperibili invocando il path /context
          required: true
          schema:
            type: string
            default: "SUAP"  

      responses:
        "200":
          description: Elenco  tuple {fattispecie,fattispecie,obbligatorio} richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - primary
                  properties:
                    primary:
                      $ref: "#/components/schemas/UseCaseProceeding"
                    secondary:
                      $ref: "#/components/schemas/UseCaseProceeding"
                    mandatory:
                      type: boolean

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /proceedings-forms/{id_proceeding_list}:
    get:
      description: |
        Recupero bulk elenco tuple  {procedimenti,moduli digitali}(tutte le versioni) per una lista di identificativi procedimento
      parameters:
        - in: path
          name: id_proceeding_list
          description:  elenco degli id per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
  
      responses:
        "200":
          description: Elenco tuple  {procedimenti,moduli digitali} richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - proceeding
                    - form
                  properties:
                    proceeding:
                      $ref: "#/components/schemas/ContextEntity"
                    form:
                      $ref: "#/components/schemas/GenericEntity"


          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /forms-schematrons/{id_form_list}:
    get:
      description: |
        Recupero bulk elenco tuple  {moduli digitali,schematron}(tutte le versioni) per una lista di identificativi procedimento
      parameters:
        - in: path
          name: id_form_list
          description:  elenco degli id per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
    


      responses:
        "200":
          description: Elenco tuple  {moduli digitali,schematron} richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - form
                    - schematron
                  properties:
                    form:
                      $ref: "#/components/schemas/GenericEntity"
                    schematron:
                      $ref: "#/components/schemas/GenericEntity"
                    
          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /forms-xsds/{id_form_list}:
    get:
      description: |
        Recupero bulk elenco tuple  {moduli digitali,xml schema}(tutte le versioni) per una lista di identificativi procedimento
      parameters:
        - in: path
          name: id_form_list
          description:  elenco degli id per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
    
      responses:
        "200":
          description: Elenco tuple  {moduli digitali,xml schema} richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - form
                    - schematron
                  properties:
                    form:
                      $ref: "#/components/schemas/GenericEntity"
                    xsd:
                      $ref: "#/components/schemas/GenericEntity"
                    
          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /proceedings-competent-administrations/{id_proceeding_list}:
    get:
      description: |
         Recupero bulk tuple {procedimento,amministrazione competente,comune}(tutte le versioni) per una lista di identificativi procedimento
      parameters:
        - in: path
          name: id_proceeding_list
          description: elenco degli id procedimento per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
      
      responses:
        "200":
          description: Elenco tuple {procedimento,amministrazione competente,comune} richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - proceeding
                    - competent_administration
                    - municipality
                  properties:
                    proceeding:
                      $ref: "#/components/schemas/ContextEntity"
                    competent_administration:
                      $ref: "#/components/schemas/GenericEntity"
                    municipality:
                      type: string

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /proceedings-competent-administrations/{id_proceeding_list}/{municipality_list}:  
    get:
      description: |
         Recupero bulk tuple {procedimento,amministrazione competente,comune}(tutte le versioni)(tutte le versioni) per una lista di identificativi procedimento
      parameters:
        - in: path
          name: id_proceeding_list
          description: elenco degli id procedimento per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
          style: simple
        - in: path
          name: municipality_list
          description: elenco dei comuni per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple

      responses:
        "200":
          description: Elenco tuple {procedimento,amministrazione competente,comune} richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - proceeding
                    - competent_administration
                    - municipality
                  properties:
                    proceeding:
                      $ref: "#/components/schemas/ContextEntity"
                    competent_administration:
                      $ref: "#/components/schemas/GenericEntity"
                    municipality:
                      type: string

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /competent-administrations-systems/{id_competent_administrations_list}:
    get:
      description: |
         Recupero bulk tuple {amministrazione competente,sistema ente terzo}(tutte le versioni) per una lista di identificativi dell'amministrazione competente
      parameters:
        - in: path
          name: id_competent_administrations_list
          description: elenco degli id per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
      
      responses:
        "200":
          description: Elenco tuple {amministrazione competente,sistema ente terzo} richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - competent_administration
                    - third_party_system
                  properties:
                    competent_administration:
                      $ref: "#/components/schemas/AdministrationEntity"
                    third_party_system:
                      $ref: "#/components/schemas/third_party_system"
              
          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /single_desks-by-municipality/{municipality_list}/{context}:
    get:
      description: |
        Recupero bulk elenco degli sportelli (tutte le versioni) per una lista di comuni
      parameters:
        - in: path
          name: municipality_list
          description: elenco dei comuni per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
        - in: path
          name: context
          description: contesto di riferimento. I valori ammissibili sono reperibili invocando il path /context
          required: true
          schema:
            type: string
            default: "SUAP"

      responses:
        "200":
          description: Elenco dei sportelli richiesti
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - single_desk
                    - municipality
                  properties:
                    single_desk:
                      $ref: "#/components/schemas/DeskEntity"
                    municipality:
                      type: string
          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /single_desks-by-id/{id_single_desk_list}:
    get:
      description: |
        Recupero bulk elenco sportelli (tutte le versioni) per una lista di identificativi sportelli
      parameters:
        - in: path
          name: id_single_desk_list
          description: elenco degli id per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple

      responses:
        "200":
          description: Elenco sportelli richiesti
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - single_desk
                  properties:
                    single_desk:
                      $ref: "#/components/schemas/DeskEntity"

          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /single_desks-fo-systems/{id_single_desk_list}:
    get:
      description: |
         Recupero bulk tuple {sportello, sistema FrontOffice}(tutte le versioni) per una lista di identificativi degli sportelli
      parameters:
        - in: path
          name: id_single_desk_list
          description: elenco degli id per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple
      
      responses:
        "200":
          description: Elenco tuple {sportello,sistema FrontOffice} richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - single_desk
                    - fo_system
                  properties:
                    single_desk:
                      $ref: "#/components/schemas/DeskEntity"
                    fo_system:
                      $ref: "#/components/schemas/FOSystemEntity"
              
          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /single_desks-bo-systems/{id_single_desk_list}:
    get:
      description: |
         Recupero bulk tuple {sportello, sistema BackOffice}(tutte le versioni) per una lista di identificativi
      parameters:
        - in: path
          name: id_single_desk_list
          description: elenco degli id per le quali la risorsa è richiesta
          required: true
          schema: 
            type: array
            items:
              type: string
            maxItems: 50
          style: simple

      responses:
        "200":
          description: Elenco tuple {sportello, sistema BackOffice} richiesto
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - single_desk
                    - bo_system
                  properties:
                    single_desk:
                      $ref: "#/components/schemas/DeskEntity"
                    bo_system:
                      $ref: "#/components/schemas/BOSystemEntity"
              
          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"

        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"

      security:
        - bearerAuth: []
          Agid-JWT-Signature: []

      tags:
        - Bulk

  /administrations-by-id/{ids}:
    get:
      description: |
        Recupero bulk elenco amministrazioni dati i codici fiscali
      parameters:
        - in: path
          name: ids
          description: elenco deigli id degli enti 
          required: true
          schema:
            type: array
            items:
              type: string
          style: simple
      responses:
        "200":
          description: Elenco delle amministrazioni richieste
          content:
            application/json:
              schema:
                type: object
                properties:
                  administrations_with_fiscal_code:
                    type: array
                    items:
                      $ref: "#/components/schemas/AdministrationFiscalCode"
                  administrations_with_ipa:
                    type: array
                    items:
                      $ref: "#/components/schemas/AdministrationSchema"
          headers:
            Agid-JWT-Signature:
              $ref: "#/components/headers/Agid-JWT-Signature"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/ServerError"
        "503":
          $ref: "#/components/responses/ServiceUnavaliable"
      security:
        - bearerAuth: []
          Agid-JWT-Signature: []
      tags:
        - Bulk

components:
  responses:
    

    BadRequest:
      description: Forma della richiesta non conforme alla specifica
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
      headers:
        Agid-JWT-Signature:
          $ref: "#/components/headers/Agid-JWT-Signature"
    ServiceUnavaliable:
      description: Servizio non disponibile
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
      headers:
        Agid-JWT-Signature:
          $ref: "#/components/headers/Agid-JWT-Signature"

    Unauthorized:
      description: Richiesta non autorizzata
      headers:
        Agid-JWT-Signature:
          $ref: "#/components/headers/Agid-JWT-Signature"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

    NotFound:
      description: risorsa non trovata
      headers:
        Agid-JWT-Signature:
          $ref: "#/components/headers/Agid-JWT-Signature"
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
            
    ServerError:
      description: Errore processamento richiesta
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
      headers:
        Agid-JWT-Signature:
          $ref: "#/components/headers/Agid-JWT-Signature"

  schemas:
    AdministrationSchema:
      description: Amministrazione compentente censita nel Catalogo SSU
      type: object
      required:
        - ipacode
        - officecode
        - version
        - id
        - description
      properties:
        ipacode:
          title: codice IPA dell'amministrazione
          type: string
          minLength: 1
        officecode:
          title: codice dell'ufficio dell'amministrazione 
          type: string
          minLength: 1
        version:
          title: versione dell'ufficio dell'amministrazione 
          type: string
          pattern: "[0-9]{2}.[0-9]{2}.[0-9]{2}"
        description:
            title: descrizione dell'amministrazione
            type: string 
        id:
          title: identificativo SSU dell'amministrazione
          type: string
          minLength: 1

    AdministrationFiscalCode:
      description: Struttura di ritorno per fiscal_code, description, id
      type: object
      required:
        - id
        - fiscal_code
        - description
      properties:
        id:
          type: string
        fiscal_code:
          type: string
        description:
          type: string


    Times:
      description: tempi amministrativi dell'istanza
      type: object
      required:
      - max_gg_proc
      properties:
        max_gg_proc:          
          title: numero di giorni limite a partire da start entro cui l'istanza deve concludersi
          type: integer
          format: int32
        max_gg_correction_req:
          title: numero di giorni limite a partire da start per l'inoltro della richiesta di correzione a valle di un controllo formale
          type: integer
          format: int32
        max_gg_correction:
          title: numero di giorni limite a partire da start per la correzione a valle di un controllo formale
          type: integer
          format: int32
        max_gg_admissibility:          
          title: numero di giorni limite a partire da start per la verifica di ammissibilità
          type: integer
          format: int32
        max_gg_int_req:          
          title: numero di giorni limite a partire da start per la richiesta di integrazione degli enti terzi
          type: integer
          format: int32
        max_gg_int_resp:          
          title: numero di giorni limite a partire da start per integrazione del soggetto presentatore
          type: integer
          format: int32
        max_gg_conf_req:          
          title: numero di giorni limite a partire da start per la richiesta di conformazione degli enti terzi
          type: integer
          format: int32
        max_gg_conf_resp:          
          title: numero di giorni limite a partire da start per conformazione del soggetto presentatore
          type: integer
          format: int32
        max_gg_concl_send:          
          title: numero di giorni limite a partire da start per inoltro delle conclusioni degli enti terzi
          type: integer
          format: int32
        max_gg_cdss_req:          
          title: numero di giorni limite a partire da start entro cui gli enti terzi possono richiedere conferenza di servizi sincrona
          type: integer
          format: int32
        max_gg_inspection:          
          title: numero di giorni limite a partire da start entro cui gli enti terzi possono effettuare un'ispezione e inviare il relativo esito
          type: integer
          format: int32
        max_gg_prov_cond_ext_req:          
          title: numero di giorni limite a partire da start entro cui gli enti terzi possono effettuare una richiesta di proroga per il provvedimento condizionato 
          type: integer
          format: int32

    Error:
      type: object
      description: codice e descrizione condizione di errore
      properties:
        code:
          type: string
        message:
          type: string
      required:
        - code
        - message

    GenericEntity:
      description: struttura di risorsa generica con id,versione e descrizione
      type: object
      required:
        - id
        - version
        - description
      properties:
        id:
          title: identificativo univoco della risorsa
          type: string
        version:
          title: version della risorsa
          type: string
        description:
          title: descrizione della risorsa
          type: string
      example:
        id: 1
        version: 1.0.0
        description: Entità generica con id,versione, descrizione
    
    ContextEntity:
      allOf:
        - $ref: "#/components/schemas/GenericEntity"
        - type: object
          required:
            - context
          properties:
            context:
              title: contesto di riferimento
              type: string
      example:
        id: 1
        version: 1.0.0
        description: Entità generica con id,versione, descrizione
        context: SUAP

    ComponentEntity:
      allOf:
        - $ref: "#/components/schemas/GenericEntity"
        - type: object
          required:
            - start
            - end
            - state
          properties:
            start:          
              title: data inizio intervallo di validità della versione
              type: string
              format: date
            end:          
              title: data fine intervallo di validità della versione
              type: string
              format: date
            state:
              title: stato di validità della versione
              type: string
              enum:
                - Active
                - Not Active
                - Revocated
      example:
        id: 1
        version: 1.0.0
        description: Entità generica con id,versione, descrizione
        start: 2024-01-01
        end: 2024-12-31
        state: Active
        
    AdministrationEntity:
      description: struttura di risorsa generica con id,versione e descrizione
      allOf:
        - type: object
          required:
            - id
            - description
          properties:
           id:
              title: identificativo univoco della risorsa
              type: string
           version:
             title: version della risorsa
             type: string
           description:
             title: descrizione della risorsa
             type: string
           start:          
             title: data inizio intervallo di validità della versione
             type: string
             format: date
           end:          
             title: data fine intervallo di validità della versione
             type: string
             format: date
           state:
             title: stato di validità della versione
             type: string
             enum:
               - Active
               - Not Active
               - Revocated
      example:
        id: 1
        version: 1.0.0
        description: Entità generica con id,versione, descrizione
        start: 2024-01-01
        end: 2024-12-31
        state: Active
    
    DeskEntity:
      description: struttura di risorsa generica con id,versione e descrizione
      allOf:
        - $ref: "#/components/schemas/ComponentEntity"
        - type: object
          required:
            - context
          properties:
            context:
              title: contesto di riferimento (es. SUAP, SUE, ZES)
              type: string

      example:
        id: 1
        version: 1.0.0
        description: Entità generica con id,versione, descrizione
        start: 2024-01-01
        end: 2024-12-31
        state: Active
        context: SUAP

    DocumentClassification:
      description: struttura oggetto classificazione dei documenti
      type: object
      required:
        - coding
        - class_code
        - class_description
        - typology_code
        - typology_description
      properties:
        coding:
          title: Codifica della classificazione associata ad un documento
          type: string
          pattern: "C[0-9]{1,2}_[a-z]"
        class_code:
          title: Codice della Classe di un documento
          type: string
          pattern: "C[0-9]{1,2}"
        class_description:
          title: Descrizione della Classe di un documento
          type: string
        typology_code:
          title: Codice della Tipologia di un documento
          type: string
          pattern: "[a-z]"
        typology_description:
          title: Descrizione della Tipologia di un documento
          type: string
      example:
        coding: "C2_b"
        class_code: "C2"
        class_description: "AUTORIZZAZIONI, PERMESSI E AUTOCERTIFICAZIONI"
        typology_code: "b"
        typology_description: "Autorizzazioni"

    DocumentEntity:
      allOf:
        - $ref: "#/components/schemas/GenericEntity"
        - type: object
          required:
            - type
          properties:
            type:
              type: string
              enum:
                - structured
                - unstructured
      example:
        id: 1
        version: 1.0.0
        description: Entità id,versione, descrizione e tipo strutturato/non strutturato
        type: structured 
    
    DocumentEntityClassified:
      type: object
      allOf:
        - $ref: "#/components/schemas/DocumentEntity"
        - type: object
      properties:
        document_classification_coding:
          type: string
          pattern: "C[0-9]{1,2}_[a-z]"
      example:
        id: 1
        version: 1.0.0
        description: Entità id,versione, descrizione e tipo strutturato/non strutturato
        type: structured 
        document_classification_coding: C1_a

    ResourceEntity:
      allOf:
        - $ref: "#/components/schemas/GenericEntity"
        - type: object
          required:
            - resource
          properties:
            resource:
              type: string
              format: byte
      example:
        id: 1
        version: 1.0.0
        description: Entita id,versione, descrizione e risorsa binaria codificata in base64
        resource: "U3dhZ2dlciByb2Nrcw=="

    SystemEntity:
      allOf:
        - $ref: "#/components/schemas/GenericEntity"
        - type: object
          required:
            - telematic_ref
          properties:
            telematic_ref:
              type: string
      example:
        id: 1
        version: 1.0.0
        description: Entita id,versione, descrizione e riferimento telemantico sistema implementante
        telematic_ref: string
    
    FOSystemEntity:
      allOf:
        - $ref: "#/components/schemas/ComponentEntity"
        - type: object
          required:
            - e_service_to_bo
          properties:
            e_service_to_bo:
              type: string
            e_service_to_cu:
              type: string
      example:
        id: 1
        version: 1.0.0
        description: Entità generica con id,versione, descrizione
        start: 2024-01-01
        end: 2024-12-31
        state: Active
        e_service_to_bo: string
        e_service_to_cu: string
      
    BOSystemEntity:
      allOf:
        - $ref: "#/components/schemas/ComponentEntity"
        - type: object
          required:
            - e_service_to_fo
            - e_service_to_et
            - e_service_to_ri
          properties:
            e_service_to_fo:
              type: string
            e_service_to_et:
              type: string  
            e_service_to_ri:
              type: string  
      example:
        id: 1
        version: 1.0.0
        description: Entità generica con id,versione, descrizione
        start: 2024-01-01
        end: 2024-12-31
        state: Active
        e_service_to_fo: string
        e_service_to_et: string  
        e_service_to_ri: string

    Proceeding:
      description: procedimento amministrativo
      allOf:
        - $ref: "#/components/schemas/GenericEntity"
        - type: object
          required:
            - typology
            - lifeevent
            - territorial_scope
            - status
            - context
          properties:
            typology:
              title: tipologia del procedimento
              type: string
            lifeevent:
              title: evento della vita dell'attività afferente al procedimento
              type: string
            territorial_scope:
              title: ambito territoriale del procedimento (codice istat a 6 cifre e 000000 per Nazionale)
              type: string
            status:
              title: stato del procedimento (attivo, non attivo)
              type: string
              enum: 
                - Active
                - Not Active
            context:
              title: contesto di riferimento (SUAP, SUE, ZES)
              type: string
            tags:
              title: id dei tags che caratterizzano i procedimenti
              type: array
              items:
                type: string            
            flag_competent_administration_special:
              title: il procedimento che gli enti competenti hanno funzioni suap
              type: boolean

      example:
        id: 1
        version: 1.0.0
        description: Entita  procedimento
        typology: esercizio_vicinato
        lifeevent: apertura
        territorial_scope: 00000000
        status: Active
        context: ZES
        flag_competent_administration_special: false

    UseCaseProceeding:
      allOf:
        - $ref: "#/components/schemas/GenericEntity"
        - type: object
          required:
            - proceeding_id
            - proceeding_version
            - proceeding_context
          properties:
            proceeding_id:
              type: string
            proceeding_version:
              type: string
            proceeding_context:
              title: contesto di riferimento (SUAP, SUE, ZES)
              type: string
            schematron_phase:
              type: string

      example:
        id: 1
        version: 1.0.0
        description: Entita  fattispecie procedimento
        proceeding_id: string
        proceeding_version: 1.0.0
        proceeding_context: SUAP
        schematron_phase: phase_1
    
    AdministrativeRegime:
      description: Regimi amministrativi dei procedimenti
      type: object
      required:
        - context
        - id
        - version
        - description
      properties:
        context:
          title: contesto cui il regime amministrativo è associato
          type: string
        id:
          title: identificativo unico del regime amministrativo recuperato da apposito servizio
          type: string
        version:
          title: version del regime amministrativo
          type: string
        description:
          title: descrizione del regime amministrativo
          type: string
      example:
        context: SUAP
        id: SCIA
        version: 1.0.0
        description: Entità regime amministrativo con id,versione, descrizione

    third_party_system:
      allOf:
        - $ref: "#/components/schemas/SystemEntity"
        - type: object
          properties:
            date_from:
              type: string
              format: date 
            date_to:
              type: string
              format: date
              
      example:
        id: 1
        version: 1.0.0
        description: Entita id,versione, descrizione e riferimento telemantico sistema implementante
        telematic_ref: string
        date_from: 2023-11-14
        date_to: 2023-12-14

  headers:
    Agid-JWT-Signature:
      schema:
        format: JWS
        maxLength: 4096
        type: string
        example: eyJzd.fafa.fafafa
      description: |-
        Il valore di questo header è una token JWT firmato secondo la specifica JWS.
        Vedi Modi [integrity_rest_02] Integrità del payload del messaggio REST.
        In ogni caso la versione del pattern di sicurezza da utilizzare è l'ultima versione indicata da PDND.

  securitySchemes:
    bearerAuth:
      scheme: bearer
      bearerFormat: JWT
      type: http
      description: Implementazione conforme ad RFC8725

    Agid-JWT-Signature:
      type: apiKey
      description: |-
        Il valore di questo header è una token JWT firmato secondo la specifica JWS.
        Vedi Modi [integrity_rest_02] Integrità del payload del messaggio REST.
        In ogni caso la versione del pattern di sicurezza da utilizzare è l'ultima versione indicata da PDND.
      name: Agid-JWT-Signature
      in: header